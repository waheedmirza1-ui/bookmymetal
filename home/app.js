const chips=[...document.querySelectorAll('.chip')];const cards=[...document.querySelectorAll('.card')];const input=document.getElementById('searchInput');chips.forEach(chip=>chip.addEventListener('click',()=>{chips.forEach(c=>c.classList.remove('active'));chip.classList.add('active');const filter=chip.dataset.filter;cards.forEach(card=>card.classList.toggle('hide',filter!=='all'&&card.dataset.category!==filter))}));input.addEventListener('input',()=>{const q=input.value.toLowerCase().trim();cards.forEach(card=>card.classList.toggle('hide',q&&!card.innerText.toLowerCase().includes(q)))});document.querySelectorAll('.compare').forEach(btn=>btn.addEventListener('click',()=>{btn.classList.toggle('selected');btn.textContent=btn.classList.contains('selected')?'✓ Added':'＋ Compare'}));document.querySelectorAll('.primary').forEach(btn=>btn.addEventListener('click',()=>{if(btn.textContent==='Enquire')alert('Enquiry flow will be connected to the backend in the next step.')}));